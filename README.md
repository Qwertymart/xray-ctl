## Master
```
sudo apt update

sudo apt install golang git

git clone https://github.com/Qwertymart/xray-ctl

cd xray-ctl

sudo go run cmd/master/main.go
```
Конфиг в config_master.yaml

## Bridge

```
sudo apt update

sudo apt install golang git

git clone https://github.com/Qwertymart/xray-ctl

cd xray-ctl

vim config_bridge.yaml

sudo go run cmd/bridge/main.go
```
